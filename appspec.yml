version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/  # Default directory for Apache HTTPD

hooks:
  BeforeInstall:
    - location: scripts/install_dependencies.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: scripts/restart.sh
      timeout: 300
      runas: root
